package com.example.demo.agent.strategy;

// The interface of StrategyAgent

import com.example.demo.agent.core.Agent;

public interface CreativeStrategistAgent
        extends Agent<StrategyRequest, StrategyResult> {
}